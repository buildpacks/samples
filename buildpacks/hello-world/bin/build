#!/usr/bin/env bash
set -eo pipefail

echo "---> Hello World buildpack"

# INPUT ARGUMENTS
platform_dir=$2
env_dir=${platform_dir}/env
layers_dir=$1
plan_path=$3

cat << EOF > $1/launch.sbom.cdx.json
{}
EOF

cat << EOF > $1/launch.toml
[[bom]]
name = "some-dep"

EOF

echo "---> Done"
