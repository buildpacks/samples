# Buildpack API version
api = "0.10"

# Buildpack ID and metadata
[buildpack]
id = "samples/bash-script"
version = "0.0.1"
name = "Bash Script Buildpack"

# Stacks (deprecated) the buildpack will work with
[[stacks]]
id = "*"
